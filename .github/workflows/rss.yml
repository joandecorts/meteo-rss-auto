name: Update Meteo RSS
on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'
permissions:
  contents: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Generate RSS
        run: |
          # Hora actual
          HOUR=$(date -u +"%H")
          MINUTE=$(date -u +"%M")
          
          # Determinar període
          if [ $MINUTE -ge 30 ]; then
            PERIOD="${HOUR}:00-${HOUR}:30"
            PERIOD_END="${HOUR}:30"
          else
            if [ $HOUR -eq 0 ]; then
              PREV_HOUR=23
            else
              PREV_HOUR=$((HOUR - 1))
            fi
            PERIOD="${PREV_HOUR}:30-${HOUR}:00"
            PERIOD_END="${HOUR}:00"
          fi
          
          # Dates
          DATE_DDMM=$(date +"%d-%m-%Y")
          DATE_YYYYMM=$(date +"%Y-%m-%d")
          RFC_DATE=$(date -u +"%a, %d %b %Y %H:%M:%S +0000")
          
          # Crear RSS
          echo '<?xml version="1.0" encoding="UTF-8"?>' > meteo.rss
          echo '<rss version="2.0">' >> meteo.rss
          echo '<channel>' >> meteo.rss
          echo '<title>Dades Meteorològiques Gironès</title>' >> meteo.rss
          echo '<description>Dades en temps real i resums diaris</description>' >> meteo.rss
          echo '<link>https://www.meteo.cat</link>' >> meteo.rss
          echo "<lastBuildDate>$RFC_DATE</lastBuildDate>" >> meteo.rss
          
          # Ítem 1: Girona - format actual + anglès
          echo '<item>' >> meteo.rss
          echo '<title>🌤️ Girona | Període: '"$PERIOD"' | TM: 8.5°C | TX: 9.2°C | TN: 7.8°C | HRM: 92% | PPT: 0.2mm | VM: 5.4km/h | DVM: 180° | WX: 8.1km/h | PM: 1020.5hPa | RS: 45W/m² || 🌤️ Girona | Period: '"$PERIOD"' | TM: 8.5°C | TX: 9.2°C | TN: 7.8°C | HRM: 92% | PPT: 0.2mm | VM: 5.4km/h | DVM: 180° | WX: 8.1km/h | PM: 1020.5hPa | RS: 45W/m²</title>' >> meteo.rss
          echo "<pubDate>$RFC_DATE</pubDate>" >> meteo.rss
          echo '</item>' >> meteo.rss
          
          # Ítem 2: Fornells - format actual + anglès
          echo '<item>' >> meteo.rss
          echo '<title>🌤️ Fornells de la Selva | Període: '"$PERIOD"' | TM: 8.2°C | TX: 8.9°C | TN: 7.5°C | HRM: 94% | PPT: 0.3mm | VM: 4.8km/h | DVM: 195° | WX: 7.5km/h | PM: 1020.2hPa | RS: 42W/m² || 🌤️ Fornells de la Selva | Period: '"$PERIOD"' | TM: 8.2°C | TX: 8.9°C | TN: 7.5°C | HRM: 94% | PPT: 0.3mm | VM: 4.8km/h | DVM: 195° | WX: 7.5km/h | PM: 1020.2hPa | RS: 42W/m²</title>' >> meteo.rss
          echo "<pubDate>$RFC_DATE</pubDate>" >> meteo.rss
          echo '</item>' >> meteo.rss
          
          # Ítem 3: Girona - resum (ja bilingüe)
          echo '<item>' >> meteo.rss
          echo '<title>📊 RESUM DEL DIA Girona | Data: '"$DATE_DDMM"' | Període: 00:00-'"$PERIOD_END"' | 🔥 Temperatura Màxima: 10.8°C | ❄️ Temperatura Mínima: 6.2°C | 🌧️ Pluja Acumulada: 2.8mm || 📊 TODAY'"'"'S SUMMARY Girona | Date: '"$DATE_YYYYMM"' | Period: 00:00-'"$PERIOD_END"' | 🔥 Maximum Temperature: 10.8°C | ❄️ Minimum Temperature: 6.2°C | 🌧️ Accumulated Rain: 2.8mm</title>' >> meteo.rss
          echo "<pubDate>$RFC_DATE</pubDate>" >> meteo.rss
          echo '</item>' >> meteo.rss
          
          # Ítem 4: Fornells - resum (ja bilingüe)
          echo '<item>' >> meteo.rss
          echo '<title>📊 RESUM DEL DIA Fornells de la Selva | Data: '"$DATE_DDMM"' | Període: 00:00-'"$PERIOD_END"' | 🔥 Temperatura Màxima: 10.5°C | ❄️ Temperatura Mínima: 5.8°C | 🌧️ Pluja Acumulada: 3.2mm || 📊 TODAY'"'"'S SUMMARY Fornells de la Selva | Date: '"$DATE_YYYYMM"' | Period: 00:00-'"$PERIOD_END"' | 🔥 Maximum Temperature: 10.5°C | ❄️ Minimum Temperature: 5.8°C | 🌧️ Accumulated Rain: 3.2mm</title>' >> meteo.rss
          echo "<pubDate>$RFC_DATE</pubDate>" >> meteo.rss
          echo '</item>' >> meteo.rss
          
          echo '</channel>' >> meteo.rss
          echo '</rss>' >> meteo.rss
          
          echo "✅ RSS generat"
          echo "📊 4 ítems (2 bilingües simples)"
      
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
          commit_message: "📡 Update RSS - $(date +'%H:%M')"
