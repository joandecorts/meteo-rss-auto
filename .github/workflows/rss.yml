name: Update Meteo RSS Automatic

on:
  # Execució cada hora als :05 per tenir un patró més previsible
  schedule:
    - cron: '5 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    # 1. DESCARREGAR EL CODI
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
        
    # 2. CONFIGURAR PYTHON
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    # 3. INSTAL·LAR DEPENDÈNCIES
    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install requests beautifulsoup4 pytz lxml
        
    # 4. AFEGIR DEBUG MÉS DETALLAT
    - name: Run your Python script
      run: |
        echo "=== INICI SCRIPT ==="
        echo "Directori actual: $(pwd)"
        echo "Contingut directori:"
        ls -la
        echo "=== EXECUTANT SCRIPT ==="
        python generate_meteo_rss.py
        echo "=== COMPROVANT RESULTAT ==="
        if [ -f "meteo.rss" ]; then
          echo "✅ meteo.rss generat"
          echo "Mida del fitxer: $(wc -l < meteo.rss) línies"
          head -5 meteo.rss
        else
          echo "❌ ERROR: No s'ha generat meteo.rss"
        fi
        
    # 5. PENJAR A GITHUB PAGES
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./
        publish_branch: gh-pages
        keep_files: true
        commit_message: "🤖 Actualització automàtica RSS - $(date -u +'%Y-%m-%d %H:%M UTC')"
