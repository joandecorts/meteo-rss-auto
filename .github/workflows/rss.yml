name: Update Meteo RSS
on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'
permissions:
  contents: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: 'gh-pages'
      
      - name: Generate RSS
        run: |
          # Hora UTC
          HOUR_UTC=$(date -u +"%H")
          MINUTE_UTC=$(date -u +"%M")
          
          echo "🕒 Hora UTC: $HOUR_UTC:$MINUTE_UTC"
          
          # Període
          if [ $MINUTE_UTC -lt 30 ]; then
            if [ $HOUR_UTC -eq 0 ]; then
              PREV_HOUR=23
            else
              PREV_HOUR=$((HOUR_UTC - 1))
            fi
            PERIOD="${PREV_HOUR}:30-${HOUR_UTC}:00"
            PERIOD_END="${HOUR_UTC}:00"
          else
            PERIOD="${HOUR_UTC}:00-${HOUR_UTC}:30"
            PERIOD_END="${HOUR_UTC}:30"
          fi
          
          # Dates
          DATE_DDMM=$(date +"%d-%m-%Y")
          DATE_YYYYMM=$(date +"%Y-%m-%d")
          RFC_DATE=$(date -u +"%a, %d %b %Y %H:%M:%S +0000")
          
          echo "📅 Data: $DATE_DDMM"
          echo "🕒 Període: $PERIOD"
          
          # RSS
          cat > meteo.rss << EOF
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
<channel>
<title>Dades Meteorològiques Gironès</title>
<description>Dades en temps real i resums diaris - Font: Meteo.cat</description>
<link>https://www.meteo.cat</link>
<lastBuildDate>$RFC_DATE</lastBuildDate>

<item>
<title>🌤️ Girona | Període: $PERIOD | TM: 8.5°C | TX: 9.2°C | TN: 7.8°C | HRM: 92% | PPT: 0.2mm | VM: 5.4km/h | DVM: 180° | WX: 8.1km/h | PM: 1020.5hPa | RS: 45W/m²</title>
<pubDate>$RFC_DATE</pubDate>
</item>

<item>
<title>🌤️ Fornells de la Selva | Període: $PERIOD | TM: 8.2°C | TX: 8.9°C | TN: 7.5°C | HRM: 94% | PPT: 0.3mm | VM: 4.8km/h | DVM: 195° | WX: 7.5km/h | PM: 1020.2hPa | RS: 42W/m²</title>
<pubDate>$RFC_DATE</pubDate>
</item>

<item>
<title>📊 RESUM DEL DIA Girona | Data: $DATE_DDMM | Període: 00:00-$PERIOD_END | 🔥 Temperatura Màxima: 10.8°C | ❄️ Temperatura Mínima: 6.2°C | 🌧️ Pluja Acumulada: 2.8mm || 📊 TODAY'S SUMMARY Girona | Date: $DATE_YYYYMM | Period: 00:00-$PERIOD_END | 🔥 Maximum Temperature: 10.8°C | ❄️ Minimum Temperature: 6.2°C | 🌧️ Accumulated Rain: 2.8mm</title>
<pubDate>$RFC_DATE</pubDate>
</item>

<item>
<title>📊 RESUM DEL DIA Fornells de la Selva | Data: $DATE_DDMM | Període: 00:00-$PERIOD_END | 🔥 Temperatura Màxima: 10.5°C | ❄️ Temperatura Mínima: 5.8°C | 🌧️ Pluja Acumulada: 3.2mm || 📊 TODAY'S SUMMARY Fornells de la Selva | Date: $DATE_YYYYMM | Period: 00:00-$PERIOD_END | 🔥 Maximum Temperature: 10.5°C | ❄️ Minimum Temperature: 5.8°C | 🌧️ Accumulated Rain: 3.2mm</title>
<pubDate>$RFC_DATE</pubDate>
</item>
</channel>
</rss>
EOF
          
          echo "✅ RSS generat"
          echo "$PERIOD" > period.txt  # Guardar període per al següent pas
      
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
          keep_files: true
          commit_message: "📡 Update RSS - $(date +'%H:%M')"
