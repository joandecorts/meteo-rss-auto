# 1. Copia aquest contingut corregit al teu fitxer
@'
[name: Update Meteo RSS and HTML Automatically

on:
  # Permetre que cron-job.org dispari l'acció (cada 5 minuts)
  repository_dispatch:
    types: [trigger_update]
  # Execució programada interna (cada 5 minuts)
  schedule:
    - cron: '*/5 * * * *'
  # Permetre execucions manuals des de la pestanya d'Accions
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    # 1. DESCARREGAR EL CODI
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
        
    # 2. CONFIGURAR PYTHON
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    # 3. INSTAL·LAR DEPENDÈNCIES
    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install requests beautifulsoup4 pytz lxml
        
    # 4. EXECUTAR TOTS ELS SCRIPTS (RSS + HTMLs)
    - name: Run all Python scripts
      run: |
        echo "=== INICI DEL PROCÉS ==="
        echo "Directori actual: $(pwd)"
        echo "Contingut inicial:"
        ls -la
        
        echo ""
        echo "=== 1. OBTENINT DADES DIÀRIES ==="
        python daily_weather_scraper.py
        
        echo ""
        echo "=== 2. GENERANT FITXERS HTML ==="
        python generate_fullscreen_html.py
        
        echo ""
        echo "=== 3. GENERANT RSS ==="
        python generate_meteo_rss.py
        
        echo ""
        echo "=== COMPROVANT RESULTATS ==="
        echo "Contingut de la carpeta 'outputs/':"
        ls -la outputs/
        
        echo ""
        if [ -f "outputs/meteo.rss" ]; then
          echo "✅ RSS generat: outputs/meteo.rss"
          echo "   Mida: $(wc -l < outputs/meteo.rss) línies"
        else
          echo "❌ ERROR: No s'ha generat outputs/meteo.rss"
        fi
        
        if [ -f "outputs/girona_full_screen.html" ] && [ -f "outputs/fornells_full_screen.html" ]; then
          echo "✅ HTMLs generats: girona_full_screen.html i fornells_full_screen.html"
        else
          echo "⚠️  AVÍS: Falten alguns fitxers HTML"
        fi
        
    # 5. PENJAR NOMÉS 'outputs/' A GITHUB PAGES
    - name: Deploy Outputs to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./outputs           # ⭐ CANVI CLAU: Només puja 'outputs/'
        publish_branch: gh-pages
        keep_files: false                # ⭐ Neteja la branca gh-pages abans
        commit_message: "🌤️ Actualització dades i HTMLs - $(date -u +'%Y-%m-%d %H:%M UTC')"
'@ | Set-Content -Path ".github\workflows\update.yml"

# 2. Verifica que s'hagi guardat bé
Get-Content ".github\workflows\update.yml" | Select-Object -First 15

# 3. Fes commit i push
git add ".github\workflows\update.yml"
git commit -m "fix: Correct Python script paths (remove scripts/ folder)"
git push origin main
