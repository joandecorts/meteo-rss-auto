# Versió super simple de prova
@'
name: Test Workflow
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World"
'@ | Set-Content ".github\workflows\update.yml" -Encoding UTF8

# Puja
git add ".github\workflows\update.yml"
git commit -m "test: Minimal workflow"
git push origin main
