name: Test Schedule

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  test-schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Test run
        run: |
          echo "✅ Schedule funciona! Timestamp: $(date)"
          echo "🕒 Hora actual: $(date +'%H:%M:%S')"